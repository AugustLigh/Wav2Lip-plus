# librosa==0.7.0
# numpy==1.17.1
# opencv-contrib-python==4.3.0.38
# opencv-python==4.3.0.38
# torch==1.1.0
# torchvision==0.3.0
# tqdm==4.45.0
# numba==0.48

# Базовые библиотеки (значительно обновлены)
numpy>=1.22.0,<1.25.0  # Современные библиотеки требуют свежий numpy
scipy>=1.8.0          # Для аудио и других операций
tqdm>=4.60.0          # Обновленная версия tqdm

# Обработка изображений/видео (используем headless для избежания конфликтов GUI)
opencv-python-headless>=4.6.0,<4.9.0 # Обновленный OpenCV

# Обработка аудио (обновлены для совместимости с numpy/numba)
librosa>=0.9.1        # Wav2Lip использует базовые функции, но обновим
numba>=0.55.0         # Зависимость librosa, требует обновления

# Детекция лиц (ключевая зависимость)
face-alignment>=1.3.5 # Библиотека, содержащая SFD детектор
# dlib>=19.20           # Раскомментируйте и установите, если face-alignment потребует